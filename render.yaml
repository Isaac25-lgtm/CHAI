databases:
  - name: chai-pmtct-db
    plan: free
    databaseName: chai_pmtct
    user: chai_user

services:
  - type: web
    name: chai-pmtct
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node .next/standalone/server.js
    # To seed the database for the first time, run manually:
    # npm run db:seed
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: chai-pmtct-db
          property: connectionString
      - key: NEXTAUTH_SECRET
        generateValue: true
      - key: NEXTAUTH_URL
        sync: false
      - key: NODE_ENV
        value: production
